// Shared by the widget input and the API route so the two limits can't drift apart
export const GUESTBOOK_MAX_LENGTH = 500;

// Show the character counter once the message gets this close to the limit
export const GUESTBOOK_COUNTER_THRESHOLD = 100;
